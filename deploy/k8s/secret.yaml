apiVersion: v1
kind: Secret
metadata:
  name: agent-os-secrets
  namespace: agent-os
type: Opaque
stringData:
  # CHANGE THESE for any non-local deployment.
  SESSION_SIGNING_SECRET: "change-me-in-production"
  POSTGRES_PASSWORD: "agentid"
  DATABASE_URL: "postgresql+psycopg2://agentid:agentid@postgres:5432/agentid"
